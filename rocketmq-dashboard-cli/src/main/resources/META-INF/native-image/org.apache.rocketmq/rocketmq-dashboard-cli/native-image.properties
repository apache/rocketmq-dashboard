# GraalVM Native Image configuration for rmqctl
Args = --initialize-at-build-time=org.apache.rocketmq.dashboard.cli \
       -H:+ReportExceptionStackTraces \
       -H:IncludeResources=.*\\.yaml$ \
       --enable-url-protocols=http,https
