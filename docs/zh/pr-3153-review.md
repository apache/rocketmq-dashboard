# PR #3153 审查修复

核实原代码已有空页退出；新增 100 页请求上限与异常短页、空页回归，保持加载失败可重试。

p95 继续使用标准数组排序，复杂度 O(n log n)，输入最多 10,000 条。此处不引入自研选择算法；错误归一化函数原本已导出。空页退出和失败重试在原实现中已经存在，本次为它们补充证据。

本地验证：13 项相关测试通过；ESLint 与 TypeScript/Vite 生产构建通过。最后验证日期：2026-09-14。

未新增依赖或数据库迁移，使用普通合并提交保留原分支历史；撤销本次提交可回滚。未运行远端工作流。
