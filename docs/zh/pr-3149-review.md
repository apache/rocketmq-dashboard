# PR #3149 审查修复

用结构化作用域字段消除展示分隔符造成的身份碰撞；按后端语法归一化复合时长，使用大整数避免精度丢失。

JSON.stringify 本身可以正确转义引号，因此保留该标准能力；真正需要修复的是将展示用分隔字符串当成作用域身份。时长遵循后端 AlertRuleDuration 的紧凑整数形式（ms/s/m/h/d/w/y），例如 1m30s 与 90s 等价，未知格式不猜测。

本地验证：13 项相关测试通过；ESLint 与 TypeScript/Vite 生产构建通过。最后验证日期：2026-09-14。

未新增依赖或数据库迁移，使用普通合并提交保留原分支历史；撤销本次提交可回滚。未运行远端工作流。
