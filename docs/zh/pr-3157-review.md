# PR #3157 审查修复

保留事件时间线入口与上游换行布局；核实原有空页退出，新增 100 页请求上限和回归。

分析最多保留 10,000 条源记录，因此事件组数量不会超过源记录数。完整加载失败仍明确显示错误并允许手动重试，不将残缺数据当成完整报告。

本地验证：28 项相关测试通过；ESLint 与 TypeScript/Vite 生产构建通过。最后验证日期：2026-09-14。

未新增依赖或数据库迁移，使用普通合并提交保留原分支历史；撤销本次提交可回滚。未运行远端工作流。
