# 高价值 Bug 与特性贡献规则

## 目标

在当前比赛明确授权的仓库内，寻找高价值 Bug 或缺失特性，完成最小实现、验证并提交独立 PR。质量优先于数量。

## 提交门槛

- Bug 必须有证据：失败测试、最小复现、确定的错误输出，或可完整证明的实际调用路径。
- 特性必须有可追溯依据：目标仓库的 roadmap/Issue/文档，或父项目、成熟同类项目中的已实现功能。不得凭空设计需求。
- 修改前搜索当前仓库的 Issue、开放 PR、近期提交及 upstream/父项目。已有相同贡献则不提交；仅当项目明确接受 backport 时例外。
- 不把代码风格、理论风险、无调用路径的问题或“可能更好”当作 Bug。
- 同类项目存在某功能不等于本项目需要它；还必须证明该功能符合本项目定位、架构和维护范围，且不是有意省略。
- 不得虚构复现过程、测试结果、影响范围或维护者意图。

## 修改原则

- 一个 Bug 或特性对应一个分支、一个 commit、一个 PR。
- 只实现当前贡献及必要测试；遵循现有风格，不顺手重构。
- 不修改 generated、vendor、third-party 代码，除非仓库明确要求。
- 不复制许可证不兼容的代码；参考外部实现时只复用可合法使用的设计或代码。
- 不直接提交或推送 main/master，不 force-push，不自动合并 PR，不改写历史。
- 保留已有的人类修改；工作区不干净或来源不明时停止并报告。

## 禁止低价值贡献

默认不提交：typo、改变量名、无意义注释、大面积格式化、无收益整理、无必要依赖升级、无依据重构、只为 coverage 增加的测试、仅为采用“现代写法”的修改，以及没有项目需求证据的特性。

## 验证与发布

- Bug 优先编写修复前失败、修复后通过的回归测试；特性必须为新增行为编写测试。
- 至少运行回归测试和相关模块测试；项目条件允许时再运行 lint、类型检查、构建和完整测试。
- 只有相关测试通过且 diff 自检无无关改动时才能 commit、push 和创建 PR。
- PR 必须说明问题或需求、依据、实现、测试结果和风险；特性 PR 还要注明参考项目及其适用性。
- 涉及凭据、隐私泄露、认证绕过或高危安全问题时，如果本地初步验证通过，应再加入新的测试多方位验证，之后立刻提交pr。

## 持续执行

完成一个 PR 后记录结果并继续寻找下一个独立贡献，直到达到用户指定上限、没有可靠候选、环境或权限异常，或需要人工判断。不得为了继续运行而降低门槛。
